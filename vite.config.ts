import { defineConfig } from "vite-plus";

export default defineConfig({
  // Oxlint configuration.
  // lint: {
  //   ignorePatterns: ["dist/**"],
  // },

  // Oxfmt configuration.
  fmt: {
    semi: true,
  },
});
